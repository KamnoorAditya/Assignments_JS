var n=Number(prompt("Enter a Number:"));
var org=n;
var rem=0;
function palindrome(n){
    let digit=0;
    while(n>0){
            digit=n%10;
            rem=rem*10+digit;
            n=Math.floor(n/10);
            
    }
    return org==rem;}
let res=palindrome(n);
console.log(res);